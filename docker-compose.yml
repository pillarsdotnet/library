services:
  library:
    build: .
    container_name: home-library
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DB_PATH=/data/library.db
    volumes:
      - library-data:/data

volumes:
  library-data:
